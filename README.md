# GOINGTOTHESIS
yahahaha
```
requirement :
    CLOUDCOMPARE:
    apt :
        sudo apt install cloudcompare
    or
        sudo apt update
        sudo apt install snapd
        sudo snap install cloudcompare
```
